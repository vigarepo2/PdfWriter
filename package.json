{
  "name": "pdfwriter",
  "version": "3.0.0",
  "description": "Professional exemption application PDF generator.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check backend/index.js && node --check frontend/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}

{
  "name": "pdfwriter",
  "version": "3.4.0",
  "description": "Mobile-first Cloudflare Worker for professional exemption application PDF drafting with exact preview-to-PDF export and stable separated assets.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": ["pdf", "cloudflare-worker", "legal-document", "jspdf", "html2canvas", "court-application", "mobile-first", "preview-editor"]
}

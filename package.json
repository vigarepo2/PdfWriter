{
  "name": "pdfwriter",
  "version": "1.0.0",
  "description": "Minimal legal exemption application PDF generator with real text PDF and scanned PDF export.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check frontend/index.js && node --check backend/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}

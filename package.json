{
  "name": "pdfwriter",
  "version": "2.1.0",
  "description": "Minimal exemption application PDF generator with real text and scanned PDF export.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check frontend/index.js && node --check backend/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}

{
  "name": "pdfwriter",
  "version": "4.0.0",
  "description": "Professional Legal Exemption Application PDF Generator — Fazilka LADC",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check backend/index.js && node --check frontend/index.js",
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  },
  "engines": {
    "node": ">=20"
  }
}

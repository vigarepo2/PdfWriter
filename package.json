{
  "name": "pdfwriter",
  "version": "3.0.0",
  "description": "A mobile-first Cloudflare Worker app for generating professional legal application PDFs with separate case title and applicant/accused handling.",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "pdf",
    "cloudflare-worker",
    "legal-document",
    "jspdf",
    "court-application",
    "mobile-first"
  ]
}
